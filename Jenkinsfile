pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                // Add build commands here, e.g., `npm install` or `mvn clean install`
            }
        }
        stage('Test') {
            steps {
                echo 'Testing...'
                // Add test commands here, e.g., `npm test` or `mvn test`
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying...'
                // Add deploy commands here, such as uploading to a server or deploying to a cloud platform
            }
        }
    }
}
